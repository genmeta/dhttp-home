# ARCHIVED

`dhttp-home` has been merged into the [`dhttp`](https://github.com/genmeta/dhttp) repository.

The two crates formerly in this repo are now in the `dhttp` workspace:
- `dhttp-identity` → `dhttp/identity/`
- `dhttp-home` → `dhttp/home/`

This repository is no longer maintained. All future development happens in `dhttp`.
